name: 'fraud_detection'
version: '1.0.0'
config-version: 2
profile: 'fraud_profile'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  fraud_detection:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
